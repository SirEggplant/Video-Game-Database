# GameShelf frontend

This is a static single-page frontend for the FastAPI service at
`http://localhost:8000`.

Start it on a CORS-enabled local port:

```bash
cd frontend
npm run start
```

Then open `http://localhost:3000`. The API must be running separately.
